<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.4//EN" "http://www.oasis-open.org/docbook/xml/4.4/docbookx.dtd">
<!--
    Copyright 2010 The myBatis Team

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
-->

<!--
    version: $Id$
-->
<chapter id="getting_started">
  <title>Getting Started</title>

  <para>
    MyBatis-Guice helps you integrate your MyBatis code seamlessly with
    Google Guice. Using the classes in this library, Google Guice will load
    the necessary MyBatis classes for you. This library also provides
    an easy way to inject MyBatis data mappers and <literal>SqlSessions</literal>
    into your application beans.
    Finally, MyBatis-Guice will let you demarcate transactions declaratively
    so you won't need to commit/rollback/close them by hand.
  </para>

  <section id="getting_started.installation">
    <title>Installation</title>
        <para>
            Installing the mybatis-guice module it is very easy, just put the
            <literal>mybatis-guice-<?eval ${project.version}?>.jar</literal> and
            dependencies in the classpath!
        </para>
        <para>
            Apache Maven users instead can easily adding the following dependency
            in their POMs :
            <programlisting language="xml"><![CDATA[<dependencies>
    ...
    <dependency>
        <groupId>org.mybatis</groupId>
        <artifactId>mybatis-guice</artifactId>
        <version>]]><?eval ${project.version}?><![CDATA[</version>
    </dependency>
    ...
</dependencies>]]></programlisting>
        </para>
  </section>

  <section id="getting_started.setup">
    <title>Quick Setup</title>
    <para>
      To use MyBatis with Guice you need to set up a <literal>MyBatisModule</literal>,
      with a <literal>DataSource</literal> at least one data mapper class and
      a transactional bean to be injected with the mapper.
    </para>

    <para>
      To setup a <literal>DataSource</literal> you can simply use the <literal>JdbcHelper</literal> (please see the
      related appendix for more informations) module to build the URL needed for your database and provide the
      connection properties:
      <programlisting language="java"><![CDATA[Properties myBatisProperties = new Properties();
myBatisProperties.setProperty("mybatis.environment.id", "test");
myBatisProperties.setProperty("JDBC.schema", "mybatis-guice_TEST");
myBatisProperties.setProperty("derby.create", "true");
myBatisProperties.setProperty("JDBC.username", "sa");
myBatisProperties.setProperty("JDBC.password", "");
myBatisProperties.setProperty("JDBC.autoCommit", "false");

Injector injector = Guice.createInjector(
    JdbcHelper.HSQLDB_Embedded,
    new Module() {
        public void configure(Binder binder) {
            Names.bindProperties(binder, myBatisProperties);
        }
    }
);]]></programlisting>
   </para>

   <para>
      Assume you have a data mapper class defined like the following:

      <programlisting language="java"><![CDATA[public interface UserMapper {

    @Select("SELECT * FROM user WHERE id = #{userId}")
    User getUser(@Param("userId") String userId);

}]]></programlisting>

      Note that the mapper class specified <emphasis>must</emphasis> be an
      interface, not an actual implementation class. In this example,
      annotations are used to specify the SQL, but a MyBatis mapper XML file
      could also be used.
   </para>

   <para>
      Assume you also have a transactional service bean that uses your mapper:

      <programlisting language="java"><![CDATA[public class FooServiceMapperImpl implements FooService {

    @Inject
    private UserMapper userMapper;

    @Transactional
    public User doSomeBusinessStuff(String userId) {
        return this.userMapper.getUser(userId);
    }

}]]></programlisting>
   </para>

   <para>
      Setup a <literal>MyBatisModule</literal>, add your mapper to it and bind also 
      your transactional service interface <literal>FooService</literal> to its implementation.
      <programlisting language="java"><![CDATA[public interface UserMapper {
...
Injector injector = Guice.createInjector(
    new MyBatisModule() {

        @Override
        protected void initialize() {
            install(JdbcHelper.HSQLDB_Embedded);
            Names.bindProperties(binder, createTestProperties());
            addMapperClasses(UserMapper.class);
            bind(FooService.class).to(FooServiceMapperImpl.class);
        }

    }
);]]></programlisting>
   </para>

    <para>
       This is all you need, you can now get an instance of your service. It will
       be automatically injected with the MyBatis mapper. With the mapper calling
       MyBatis data methods is only one line of code. Besides, all operations will 
       be transactional, that means that you will not need to commit or rollback 
       any connection. 
      <programlisting language="java"><![CDATA[FooService fooService = this.injector.getInstance(FooService.class);
fooService.doSomeBusinessStuff("data");]]></programlisting>
     </para>
  </section>
</chapter>
